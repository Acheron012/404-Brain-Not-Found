# Local frontend dev talking to local Django:
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
# If frontend runs in Docker and Django runs on host machine:
# VITE_API_PROXY_TARGET=http://host.docker.internal:8000
VITE_API_BASE_URL=

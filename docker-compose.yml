services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: schedly-frontend
    ports:
      - "5173:5173"
    environment:
      VITE_API_PROXY_TARGET: "http://host.docker.internal:8000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
